export default function LearningsPage() {
  return (
    <div className="p-6 bg-night/30 min-h-screen">
      <h1 className="text-xl font-mono text-magenta">NeonLedger — Learnings</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        OOS lever scores (strategy / source / feature / llm-calibration) ·{" "}
        <code className="text-mint">scoreboard</code> table
      </p>
    </div>
  );
}
