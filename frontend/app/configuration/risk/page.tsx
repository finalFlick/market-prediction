export default function RiskConfigPage() {
  return (
    <div className="p-6">
      <h1 className="font-mono text-foxfire">RiskGate — limits</h1>
      <p className="text-sm text-muted-foreground mt-2">Surface of `configs/risk.yaml` via `config_kv`.</p>
    </div>
  );
}
