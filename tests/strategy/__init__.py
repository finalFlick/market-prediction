"""Strategy-level tests.

Run a strategy through the engine on synthetic data and assert behavior
(target weights, no crashes, monotonic equity in trivial cases). Heavier
than unit tests, lighter than the full e2e marker.
"""
